# Tasklet vs Chunk パフォーマンス比較 - アーキテクチャ図

## 📊 システム全体構成

```mermaid
graph TB
    subgraph "Spring Boot Application"
        Runner[BatchJobRunner]
        
        subgraph "Taskletモデル（低速）"
            TJob[TaskletPerformanceJob]
            TStep[TaskletPerformanceStep]
            TTasklet[SingleInsertTasklet]
            TListener1[PerformanceJobListener]
        end
        
        subgraph "Chunkモデル（高速）"
            CJob[ChunkPerformanceJob]
            CStep[ChunkPerformanceStep]
            CReader[DummyDataItemReader]
            CWriter[DummyDataBatchItemWriter]
            CListener1[PerformanceJobListener]
        end
        
        subgraph "データベース層"
            JdbcTemplate[JdbcTemplate]
            H2DB[(H2 Database)]
        end
    end
    
    Runner -->|実行| TJob
    Runner -->|実行| CJob
    
    TJob --> TListener1
    TJob --> TStep
    TStep --> TTasklet
    TTasklet -->|1件ずつInsert| JdbcTemplate
    
    CJob --> CListener1
    CJob --> CStep
    CStep --> CReader
    CStep --> CWriter
    CReader -->|データ生成| CWriter
    CWriter -->|バルクInsert| JdbcTemplate
    
    JdbcTemplate --> H2DB
```

---

## 🔴 Taskletモデル処理フロー（低速パターン）

```mermaid
sequenceDiagram
    participant Runner as BatchJobRunner
    participant Listener as PerformanceJobListener
    participant Job as TaskletPerformanceJob
    participant Step as TaskletPerformanceStep
    participant Tasklet as SingleInsertTasklet
    participant JDBC as JdbcTemplate
    participant DB as H2 Database
    
    Runner->>Job: run()
    Job->>Listener: beforeJob() - 開始時刻記録
    Job->>Step: execute()
    Step->>Tasklet: execute()
    
    Note over Tasklet: forループ開始（100万回）
    
    loop 100万回繰り返し
        Tasklet->>Tasklet: ダミーデータ生成
        Tasklet->>JDBC: update(sql, data)
        JDBC->>DB: INSERT 1件
        DB-->>JDBC: OK
        JDBC-->>Tasklet: 完了
        
        alt 10%ごと
            Tasklet->>Tasklet: 進捗ログ出力
        end
    end
    
    Note over Tasklet: forループ終了
    
    Tasklet-->>Step: RepeatStatus.FINISHED
    Step-->>Job: 完了
    Job->>Listener: afterJob() - 実行時間計算
    Listener->>Listener: 実行時間ログ出力
    Job-->>Runner: 完了
```

### Taskletモデルの特徴

**処理方式**:
```java
for (int i = 1; i <= 1000000; i++) {
    String data = "TaskletData-" + i;
    jdbcTemplate.update(
        "INSERT INTO dummy_record (data_value, created_at) VALUES (?, ?)",
        data, LocalDateTime.now()
    );
}
```

**問題点**:
- ✗ 100万回のDB往復
- ✗ 100万回のトランザクション
- ✗ 100万回のSQLパース
- ✗ ネットワーク/I/Oオーバーヘッド大

**予想実行時間**: 60〜120秒

---

## 🟢 Chunkモデル処理フロー（高速パターン）

```mermaid
sequenceDiagram
    participant Runner as BatchJobRunner
    participant Listener as PerformanceJobListener
    participant Job as ChunkPerformanceJob
    participant Step as ChunkPerformanceStep
    participant Reader as DummyDataItemReader
    participant Writer as DummyDataBatchItemWriter
    participant JDBC as JdbcTemplate
    participant DB as H2 Database
    
    Runner->>Job: run()
    Job->>Listener: beforeJob() - 開始時刻記録
    Job->>Step: execute()
    
    Note over Step: チャンク処理開始（100チャンク）
    
    loop 100回繰り返し（1万件 × 100）
        Step->>Reader: read() × 10,000回
        
        loop チャンクサイズ分（1万件）
            Reader->>Reader: ダミーデータ生成
            Reader-->>Step: データ1件
        end
        
        Note over Step: チャンク完成（1万件）
        
        Step->>Writer: write(chunk)
        Writer->>JDBC: batchUpdate(sql, items)
        JDBC->>DB: BATCH INSERT 1万件
        DB-->>JDBC: OK
        JDBC-->>Writer: 完了
        Writer->>Writer: 進捗ログ出力
        Writer-->>Step: 完了
        
        Note over Step: コミット
    end
    
    Note over Step: チャンク処理終了
    
    Step-->>Job: 完了
    Job->>Listener: afterJob() - 実行時間計算
    Listener->>Listener: 実行時間ログ出力
    Job-->>Runner: 完了
```

### Chunkモデルの特徴

**処理方式**:
```java
// Reader: 1件ずつ読み込み（1万回）
public String read() {
    if (currentCount >= 1000000) return null;
    currentCount++;
    return "ChunkData-" + currentCount;
}

// Writer: 1万件まとめてバルクInsert
public void write(Chunk<String> chunk) {
    jdbcTemplate.batchUpdate(sql, chunk.getItems(), ...);
}
```

**利点**:
- ✓ 100回のDB往復（1/10,000）
- ✓ 100回のトランザクション（1/10,000）
- ✓ バッチ更新による最適化
- ✓ ネットワーク/I/Oオーバーヘッド小

**予想実行時間**: 5〜15秒

---

## 📊 パフォーマンス比較

```mermaid
graph LR
    subgraph "Taskletモデル"
        T1[100万回ループ] --> T2[100万回DB往復]
        T2 --> T3[100万回トランザクション]
        T3 --> T4[実行時間: 60-120秒]
    end
    
    subgraph "Chunkモデル"
        C1[100チャンク] --> C2[100回DB往復]
        C2 --> C3[100回トランザクション]
        C3 --> C4[実行時間: 5-15秒]
    end
    
    T4 -.比較.-> C4
    
    style T4 fill:#ffcccc
    style C4 fill:#ccffcc
```

### 性能差の要因

| 項目 | Taskletモデル | Chunkモデル | 改善率 |
|------|--------------|------------|--------|
| **DB往復回数** | 1,000,000回 | 100回 | **10,000倍** |
| **トランザクション回数** | 1,000,000回 | 100回 | **10,000倍** |
| **SQLパース回数** | 1,000,000回 | 100回 | **10,000倍** |
| **予想実行時間** | 60-120秒 | 5-15秒 | **8-12倍高速** |

---

## 🏗️ クラス構成図

```mermaid
classDiagram
    class BatchJobRunner {
        -JobLauncher jobLauncher
        -Job taskletPerformanceJob
        -Job chunkPerformanceJob
        +run(String[] args)
        -getJobMode(String[] args) String
    }
    
    class PerformanceJobListener {
        -long startTime
        +beforeJob(JobExecution)
        +afterJob(JobExecution)
    }
    
    class TaskletPerformanceJobConfig {
        +taskletPerformanceJob() Job
        +taskletPerformanceStep() Step
    }
    
    class SingleInsertTasklet {
        -JdbcTemplate jdbcTemplate
        -int dataCount
        +execute(StepContribution, ChunkContext) RepeatStatus
    }
    
    class ChunkPerformanceJobConfig {
        -int chunkSize
        +chunkPerformanceJob() Job
        +chunkPerformanceStep() Step
    }
    
    class DummyDataItemReader {
        -int dataCount
        -int currentCount
        +read() String
    }
    
    class DummyDataBatchItemWriter {
        -JdbcTemplate jdbcTemplate
        -int totalWriteCount
        +write(Chunk~String~)
    }
    
    BatchJobRunner --> TaskletPerformanceJobConfig
    BatchJobRunner --> ChunkPerformanceJobConfig
    
    TaskletPerformanceJobConfig --> SingleInsertTasklet
    TaskletPerformanceJobConfig --> PerformanceJobListener
    
    ChunkPerformanceJobConfig --> DummyDataItemReader
    ChunkPerformanceJobConfig --> DummyDataBatchItemWriter
    ChunkPerformanceJobConfig --> PerformanceJobListener
    
    SingleInsertTasklet --> JdbcTemplate
    DummyDataBatchItemWriter --> JdbcTemplate
```

---

## 🗄️ データベーススキーマ

```mermaid
erDiagram
    DUMMY_RECORD {
        BIGINT id PK "AUTO_INCREMENT"
        VARCHAR data_value "NOT NULL"
        TIMESTAMP created_at "NOT NULL"
    }
    
    BATCH_LOG {
        BIGINT id PK "AUTO_INCREMENT"
        VARCHAR batch_name "NOT NULL"
        TEXT message
        VARCHAR status "NOT NULL"
        TIMESTAMP created_at "NOT NULL"
    }
    
    BATCH_JOB_INSTANCE {
        BIGINT job_instance_id PK
        VARCHAR job_name
    }
    
    BATCH_JOB_EXECUTION {
        BIGINT job_execution_id PK
        BIGINT job_instance_id FK
        TIMESTAMP start_time
        TIMESTAMP end_time
        VARCHAR status
    }
    
    BATCH_STEP_EXECUTION {
        BIGINT step_execution_id PK
        BIGINT job_execution_id FK
        VARCHAR step_name
        BIGINT read_count
        BIGINT write_count
        BIGINT commit_count
    }
    
    BATCH_JOB_EXECUTION ||--|| BATCH_JOB_INSTANCE : belongs_to
    BATCH_STEP_EXECUTION ||--|| BATCH_JOB_EXECUTION : belongs_to
```

### テーブル説明

**dummy_record**:
- パフォーマンス比較用のダミーデータテーブル
- 100万件のデータが格納される

**batch_log**:
- アプリケーション独自のバッチログ（既存）

**BATCH_JOB_INSTANCE / EXECUTION / STEP_EXECUTION**:
- Spring Batchメタデータテーブル（自動生成）
- Job/Stepの実行履歴を記録

---

## 🚀 実行フロー全体像

```mermaid
flowchart TD
    Start([アプリケーション起動]) --> Init[Spring Boot初期化]
    Init --> Schema[schema.sql実行]
    Schema --> Runner[BatchJobRunner起動]
    
    Runner --> Mode{実行モード判定}
    
    Mode -->|tasklet| TaskletOnly[Taskletのみ実行]
    Mode -->|chunk| ChunkOnly[Chunkのみ実行]
    Mode -->|both| Both[両方実行]
    
    TaskletOnly --> TJob[TaskletPerformanceJob]
    ChunkOnly --> CJob[ChunkPerformanceJob]
    Both --> TJob
    Both --> Wait[2秒待機]
    Wait --> CJob
    
    TJob --> TStart[開始時刻記録]
    TStart --> TProcess[100万回ループ処理]
    TProcess --> TEnd[終了時刻記録]
    TEnd --> TLog[実行時間ログ出力]
    
    CJob --> CStart[開始時刻記録]
    CStart --> CProcess[100チャンク処理]
    CProcess --> CEnd[終了時刻記録]
    CEnd --> CLog[実行時間ログ出力]
    
    TLog --> Complete
    CLog --> Complete
    
    Complete([処理完了])
    
    style TProcess fill:#ffcccc
    style CProcess fill:#ccffcc
    style Complete fill:#ccccff
```

---

## 📈 期待される出力例

### Taskletモデル実行ログ

```
========================================
Job開始: TaskletPerformanceJob
開始時刻: Sat Apr 04 10:45:00 JST 2026
========================================

========================================
Taskletモデル処理開始
処理件数: 1000000件
処理方式: 1件ずつInsert（低速パターン）
========================================

[Tasklet進捗] 10% 完了 (100,000 / 1,000,000 件)
[Tasklet進捗] 20% 完了 (200,000 / 1,000,000 件)
[Tasklet進捗] 30% 完了 (300,000 / 1,000,000 件)
...
[Tasklet進捗] 100% 完了 (1,000,000 / 1,000,000 件)

========================================
Taskletモデル処理完了
総処理件数: 1000000件
========================================

========================================
Job終了: TaskletPerformanceJob
終了時刻: Sat Apr 04 10:46:30 JST 2026
実行時間: 90000 ms (90.0 秒)
ステータス: COMPLETED
========================================
```

### Chunkモデル実行ログ

```
========================================
Job開始: ChunkPerformanceJob
開始時刻: Sat Apr 04 10:46:32 JST 2026
========================================

========================================
Chunkモデル設定
チャンクサイズ: 10000件
処理方式: バルクInsert（高速パターン）
========================================

[Reader進捗] 100,000 件読み込み完了
[Writer進捗] 10,000 件書き込み完了（チャンクサイズ: 10,000 件）
[Writer進捗] 20,000 件書き込み完了（チャンクサイズ: 10,000 件）
...
[Reader進捗] 1,000,000 件読み込み完了
[Writer進捗] 1,000,000 件書き込み完了（チャンクサイズ: 10,000 件）

========================================
Job終了: ChunkPerformanceJob
終了時刻: Sat Apr 04 10:46:42 JST 2026
実行時間: 10000 ms (10.0 秒)
ステータス: COMPLETED
========================================
```

### 比較結果

```
╔════════════════════════════════════════════════════════╗
║   パフォーマンス比較完了                               ║
║   上記のログから実行時間を比較してください             ║
╚════════════════════════════════════════════════════════╝

【結果】
- Taskletモデル: 90.0秒
- Chunkモデル: 10.0秒
- 性能差: 9倍高速化

【結論】
バルクInsertを使用したChunkモデルが圧倒的に高速！
```

---

## 🎓 学習ポイント

### 1. なぜChunkモデルが速いのか？

```mermaid
graph TD
    A[性能差の要因] --> B[DB往復回数の削減]
    A --> C[トランザクション回数の削減]
    A --> D[バッチ更新の最適化]
    
    B --> B1[Tasklet: 100万回]
    B --> B2[Chunk: 100回]
    
    C --> C1[Tasklet: 100万回コミット]
    C --> C2[Chunk: 100回コミット]
    
    D --> D1[Tasklet: 個別SQL実行]
    D --> D2[Chunk: バッチSQL実行]
    
    style B2 fill:#ccffcc
    style C2 fill:#ccffcc
    style D2 fill:#ccffcc
```

### 2. 実務での適用

| シナリオ | 推奨モデル | 理由 |
|---------|-----------|------|
| 大量データ一括登録 | **Chunk** | バルク処理で高速 |
| 複雑な条件分岐 | Tasklet | 柔軟な制御が可能 |
| ETL処理 | **Chunk** | Reader/Writer分離 |
| 単純な集計処理 | Tasklet | シンプルな実装 |

### 3. チューニングポイント

**Chunkサイズの選択**:
- 小さすぎる（100件）: トランザクション回数増加
- 適切（10,000件）: バランス良好
- 大きすぎる（100,000件）: メモリ不足リスク

---

## 📝 まとめ

この実装により、以下を実証できます：

1. **アーキテクチャの違いによる性能差**
   - Tasklet（1件ずつ）vs Chunk（バルク）
   - 約8〜12倍の性能差

2. **Spring Batchのベストプラクティス**
   - 大量データ処理にはChunkモデル
   - バルク処理の重要性

3. **実務での選択基準**
   - データ量に応じたモデル選択
   - パフォーマンス要件の考慮

次のステップ: **Codeモード**で実装を進めてください！
